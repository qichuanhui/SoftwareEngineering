#pragma once
#include<iostream>
#include <fstream>
#include <sstream>
#include<algorithm>
using namespace std;

void init_shudu();
bool check(int n, int key);
void calculation(int n);
void solve_shudu(char* path);
